import { notFound } from 'next/navigation';

export default function Post({ params }) {
    const { post } = params;
    let postData = null;

    return (
        <main className="">
        </main>
    );
}